# Project TODO

- [ ] Add a secure server-only OpenRouter configuration for `openai/gpt-4o-mini`.
- [x] Implement a validated `generatePrDescription` tRPC procedure returning title, summary, changes, and testing notes.
- [x] Build the asymmetric International Typographic Style landing page with a git-diff textarea and the exact action label `Generate PR Description.`
- [x] Create a formatted four-section output panel with loading, error, empty, and copy-to-clipboard states.
- [x] Write unit tests for input validation and structured PR-description generation.
- [ ] Finalize the assignment-compliant README with production frontend and backend URLs after publishing.
- [x] Verify the application locally and in the externally accessible preview environment.
- [x] Create a GitHub repository, push the feature branch, and open a self-review pull request.
- [ ] Record and upload a public Google Drive demonstration video covering the live app and code ownership.
- [x] Replace the paid-model requirement with a documented no-cost server-side model option and zero-cost calculation.
- [ ] Validate an active OpenRouter free-tier API key for the `openrouter/free` server-only integration.
- [ ] Verify the existing credential via an actual `openrouter/free` PR-description request instead of a models-list request.
- [x] Add a public `/api/health` endpoint for the deployed backend verification link.
- [ ] Deploy the application to production hosting and replace preview URLs with production frontend and backend URLs.
- [ ] Make the repository and pull request accessible to reviewers without requiring GitHub authentication.
